services:
  app:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - FOLIO_PORT=${FOLIO_PORT:-8000}
    ports:
      - "${FOLIO_PORT:-8000}:8000"
    command: sh -c "php seed.php && php -S 0.0.0.0:8000 -t public/"
